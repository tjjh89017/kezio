name: Build and ingest the full-deploy test image

description: >-
  Builds the OS test image the full-deploy e2e lane writes to a target
  VM's disk over BitTorrent: downloads an Ubuntu minimal cloud image
  (UEFI/GPT bootable, ~250MiB compressed - small enough to keep
  this lane's own surface minimal), injects qemu-guest-agent into it
  offline, converts it to raw, then uploads it, requests an ImageImport,
  and waits for the Image that import creates to reach Ready.

  The runner sends bytes only. The import slices, classifies, and names
  every partition in the cluster, exactly once - so this action does no
  partclone run, no blkid probe, and no partition-table read of its own.

  The same offline virt-customize call also writes the caller's marker
  token to marker-path in the image, and this action reports the token and
  that path as outputs. A later step reads the file back out of the booted
  guest through the guest agent: the token is new on every CI run and the
  target disk starts blank, so reading it back is proof that the guest
  runs content this run wrote. The claim is about the run, so the token
  comes from the caller and every lane of one run passes the same value;
  this action never makes one of its own.
  The guest agent is baked in at build time rather
  than installed by a deploy-time hook: the provisioning network a
  deployed guest boots into has no guaranteed internet egress, so
  package installation belongs on the runner (which already needs
  network access to fetch the base image) rather than added as a second,
  less reliable network dependency inside the deploy path itself.

  Needs a KubeVirt-capable, KVM-usable runner for the guest-agent
  injection step (virt-customize boots a supermin appliance under KVM):
  the calling job must make /dev/kvm usable before this action runs (see
  prepare-kvm-host, already required by every lane that starts a real
  VM).

inputs:
  image-name:
    description: >-
      Name to give the ImageImport, the Image it creates, the content
      prefix its partitions are named under, and the image-service
      upload - all four take this one name.
    required: false
    default: "kezio-e2e-deploy-test"
  marker-path:
    description: >-
      Guest path the per-run marker token is written to at build time. Its
      parent directory must already exist in the base image: virt-customize
      --write creates the file, not the path to it.
    required: false
    default: "/etc/kezio-build-id"
  namespace:
    description: 'Namespace the image-service Service and the Image live in.'
    required: false
    default: "kezio-system"
  token:
    description: Bearer token kezioctl authenticates the upload with.
    required: true
  marker-token:
    description: >-
      Marker token to bake into the image at marker-path. Required, with no
      fallback to a token this action makes itself: a default would let a
      caller that forgets it get a lane-local token back, which is the drift
      that made the token one-per-run in the first place. One run must write
      one token, so the run - not this action - owns the value.
    required: true
  image-service-service-name:
    description: 'Name of the image-service Service to port-forward.'
    required: false
    default: "kezio-image-service"
  local-port:
    description: 'Local port the image-service Service is port-forwarded to.'
    required: false
    default: "18080"
  wait-timeout-seconds:
    description: >-
      How long to poll for the Image to reach Ready before failing. Higher
      than ingest-test-image's default: this image carries real partition
      content (hundreds of MiB), not a 2MiB fixture.
    required: false
    default: "600"
  cloud-image-url:
    description: >-
      URL of the pristine Ubuntu minimal cloud image to download. The file
      name and the release directory the SHA256SUMS list is read from are
      both taken from this one URL, so nothing else has to be changed to
      move this action to a different image or release.
    required: false
    default: "https://cloud-images.ubuntu.com/minimal/releases/noble/release/ubuntu-24.04-minimal-cloudimg-amd64.img"
  cloud-image-sha256:
    description: >-
      Optional sha256 to pin cloud-image-url's content to. Leave it empty
      (the default) to trust the SHA256SUMS list the release directory
      publishes beside the image. Set it only to hold this action on one
      specific build: the URL points at a rolling "latest build" location,
      so a pinned value fails every run after the next republish, which is
      the failure the empty default removes.
    required: false
    default: ""

outputs:
  image-name:
    description: 'Name of the Image object created (this action''s own image-name input, echoed back for a downstream step that only has this action''s outputs to work from).'
    value: ${{ inputs.image-name }}
  marker-path:
    description: 'Guest path the marker token was written to (this action''s own marker-path input, echoed back so a verifying step needs no second copy of it).'
    value: ${{ inputs.marker-path }}
  marker-token:
    description: 'Marker token baked into the image for this run (this action''s own marker-token input, echoed back so a verifying step needs no second copy of it). A verifying step reads marker-path back out of the booted guest and compares it against this value.'
    value: ${{ inputs.marker-token }}

runs:
  using: composite
  steps:
    # qemu-utils supplies qemu-img for the raw conversion below, and
    # libguestfs-tools supplies virt-customize for the offline injection.
    # Nothing here reads the partition table or slices a partition any
    # more, so partclone is not installed.
    - name: Install qemu-utils and libguestfs-tools (not preinstalled on the runner)
      shell: bash
      run: |
        set -euo pipefail
        sudo apt-get update -qq
        sudo apt-get install -y -qq --no-install-recommends qemu-utils libguestfs-tools

    - name: Build kezioctl
      shell: bash
      run: go build -o bin/kezioctl ./cmd/kezioctl

    # The expected checksum comes from the SHA256SUMS list the release
    # directory publishes beside the image, not from a value written into
    # this file. cloud-image-url points at Ubuntu's "latest build"
    # location, which is republished in place, so a checksum kept here
    # goes stale on its own and turns every lane that starts a VM red
    # with nothing changed in the repository.
    #
    # Be clear about what this check proves: the bytes match what the
    # server publishes for that file right now. It is not proof that the
    # build is one anyone reviewed. That is weaker than a real pin - but
    # the URL was already rolling, so this is the trust level the action
    # already operated at; the value kept here never pinned the build, it
    # only made the mismatch fail loudly.
    - name: Read the base image's published checksum
      id: base-image
      shell: bash
      env:
        CLOUD_IMAGE_URL: ${{ inputs.cloud-image-url }}
        CLOUD_IMAGE_SHA256: ${{ inputs.cloud-image-sha256 }}
      run: |
        set -euo pipefail

        image_file="${CLOUD_IMAGE_URL##*/}"
        sums_url="${CLOUD_IMAGE_URL%/*}/SHA256SUMS"

        if [ -n "${CLOUD_IMAGE_SHA256}" ]; then
          echo "cloud-image-sha256 is set: ${image_file} is pinned to ${CLOUD_IMAGE_SHA256}, and ${sums_url} is not read"
          sha256="${CLOUD_IMAGE_SHA256}"
        else
          sums_file="${RUNNER_TEMP}/SHA256SUMS"
          if ! curl -fsSL --retry 3 --max-time 60 -o "${sums_file}" "${sums_url}"; then
            echo "::error::could not download ${sums_url}; without it the base image has no expected checksum, so this action does not download it" >&2
            exit 1
          fi
          # Every line is "<sha256> *<file name>" (binary mode) or
          # "<sha256>  <file name>"; the leading "*" is not part of the name.
          sha256="$(awk -v want="${image_file}" '{ name = $2; sub(/^\*/, "", name); if (name == want) { print $1; exit } }' "${sums_file}")"
          if [ -z "${sha256}" ]; then
            echo "::error::${sums_url} has no line for '${image_file}'; refusing to download it unverified. Check that cloud-image-url still names a file that release directory publishes." >&2
            exit 1
          fi
          echo "${sums_url} gives ${image_file} the checksum ${sha256}"
        fi

        echo "file-name=${image_file}" >>"${GITHUB_OUTPUT}"
        echo "sha256=${sha256}" >>"${GITHUB_OUTPUT}"

    # The checksum is part of the key, so a republished image is a miss
    # rather than a stale hit. No restore-keys: a near match is a
    # different build, which is exactly what must not be restored. The
    # cached copy stays pristine - the customized image is a separate
    # working copy below - so the post-job save cannot write a
    # virt-customize output back under the pristine image's key.
    - name: Restore the base cloud image from the runner cache
      id: cloud-image-cache
      uses: actions/cache@v6
      with:
        path: ${{ runner.temp }}/cloud-image
        key: cloud-image-${{ steps.base-image.outputs.file-name }}-${{ steps.base-image.outputs.sha256 }}

    # The manifest is fetched with the image and cached with it: the two
    # must describe one build, and both come from the same rolling
    # location.
    - name: Download the base cloud image and its package manifest
      if: steps.cloud-image-cache.outputs.cache-hit != 'true'
      shell: bash
      env:
        CLOUD_IMAGE_URL: ${{ inputs.cloud-image-url }}
        IMAGE_FILE: ${{ steps.base-image.outputs.file-name }}
      run: |
        set -euo pipefail
        cache_dir="${RUNNER_TEMP}/cloud-image"
        mkdir -p "${cache_dir}"
        curl -fL --retry 3 --max-time 300 -o "${cache_dir}/${IMAGE_FILE}" "${CLOUD_IMAGE_URL}"
        curl -fsSL --retry 3 --max-time 60 \
          -o "${cache_dir}/${IMAGE_FILE%.img}.manifest" "${CLOUD_IMAGE_URL%.img}.manifest"

    # Runs on a hit as well as a miss: a restored cache entry is not
    # self-evidently intact, and it is the only copy nothing verified on
    # the way in.
    - name: Verify the base cloud image and take a working copy
      shell: bash
      env:
        IMAGE_FILE: ${{ steps.base-image.outputs.file-name }}
        EXPECTED_SHA256: ${{ steps.base-image.outputs.sha256 }}
        CACHE_HIT: ${{ steps.cloud-image-cache.outputs.cache-hit }}
      run: |
        set -euo pipefail

        cache_dir="${RUNNER_TEMP}/cloud-image"
        image="${cache_dir}/${IMAGE_FILE}"
        manifest="${cache_dir}/${IMAGE_FILE%.img}.manifest"

        for file in "${image}" "${manifest}"; do
          if [ ! -f "${file}" ]; then
            echo "::error::${file} is missing after the download/restore step (cache-hit=${CACHE_HIT:-false})" >&2
            exit 1
          fi
        done

        if ! echo "${EXPECTED_SHA256}  ${image}" | sha256sum -c -; then
          if [ "${CACHE_HIT}" = "true" ]; then
            echo "::error::the cached copy of ${IMAGE_FILE} does not match ${EXPECTED_SHA256}; the cache entry is damaged. Delete the cache entry for this key and run the lane again." >&2
          else
            echo "::error::the downloaded ${IMAGE_FILE} does not match ${EXPECTED_SHA256}, the checksum its own release directory publishes for it. The download is incomplete or the file changed while it ran; run the lane again before treating this as tampering." >&2
          fi
          exit 1
        fi

        # The steps below customize the image in place, so they work on a
        # copy and leave the cached bytes as downloaded.
        cp "${image}" "${RUNNER_TEMP}/base.img"
        cp "${manifest}" "${RUNNER_TEMP}/base.manifest"

    - name: Inject qemu-guest-agent and the marker token offline
      # --no-network + dpkg -i rather than --network + --install: booting
      # the appliance's networking hit unresolvable passt/DHCP failures
      # in a runner environment in the past (see this repo's history for
      # the debugging trail), so every package the chroot needs is
      # resolved and downloaded from the runner's own network access
      # first, then installed into the guest with no network of its own.
      #
      # qemu-guest-agent's Depends: is not fully covered by the minimal
      # cloud image's own package set (e.g. liburing2/libnuma1), which
      # would otherwise crash-loop the daemon on start. A fixed
      # dependency list would only be a stopgap against future version
      # bumps, so the closure is resolved dynamically via `apt-get
      # install --simulate` (apt's real solver, resolving `a | b`
      # alternatives and virtual packages) with
      # `-o Dir::State::status=/dev/null` so it reflects
      # qemu-guest-agent's complete dependency tree, not whatever this
      # runner already has installed. What is actually missing from the
      # image is found by diffing that closure against the image's own
      # sibling .manifest file (dpkg-query -W format), not assumed.
      #
      # No "systemctl enable" step: the shipped unit
      # (BindsTo=dev-virtio\x2dports-org.qemu.guest_agent.0.device) has
      # no [Install] section - it is systemd device-activated, starting
      # itself the moment the guest's virtio-serial guest-agent channel
      # appears (which KubeVirt always exposes), with nothing to enable.
      #
      # --write places the marker token in the same call: it is a plain
      # file write inside the appliance and needs no network, so it stays
      # beside the offline install rather than adding a second appliance
      # boot.
      shell: bash
      env:
        MARKER_PATH: ${{ inputs.marker-path }}
        MARKER_TOKEN: ${{ inputs.marker-token }}
      run: |
        set -euo pipefail

        # mapfile reading straight from a process substitution can't see a
        # failure inside it (set -o pipefail doesn't reach across <(...)),
        # so the solver's own exit status is captured explicitly first.
        qga_closure_out="$(mktemp)"
        if ! apt-get install --simulate -o Dir::State::status=/dev/null \
          --no-install-recommends qemu-guest-agent > "${qga_closure_out}"; then
          echo "::error::apt-get install --simulate failed to resolve qemu-guest-agent's dependency closure" >&2
          exit 1
        fi
        mapfile -t qga_closure < <(awk '/^Inst /{n=$2; sub(/:.*$/, "", n); print n}' "${qga_closure_out}" | sort -u)

        qga_missing_out="$(mktemp)"
        if ! comm -23 \
          <(printf '%s\n' "${qga_closure[@]}" | sort -u) \
          <(awk -F'\t' '{n=$1; sub(/:.*$/, "", n); print n}' "${RUNNER_TEMP}/base.manifest" | sort -u) \
          > "${qga_missing_out}"; then
          echo "::error::comm failed while diffing qemu-guest-agent's dependency closure against the base image manifest" >&2
          exit 1
        fi
        mapfile -t qga_missing < "${qga_missing_out}"
        if ! printf '%s\n' "${qga_missing[@]:-}" | grep -qxF qemu-guest-agent; then
          qga_missing+=("qemu-guest-agent")
        fi
        echo "missing from the image manifest, to be downloaded and injected (${#qga_missing[@]} packages): ${qga_missing[*]}"

        qga_deb_dir="${RUNNER_TEMP}/qga-debs"
        mkdir -p "${qga_deb_dir}"
        (
          cd "${qga_deb_dir}"
          for pkg in "${qga_missing[@]}"; do
            if ! apt-get download "${pkg}"; then
              echo "::error::apt-get download failed for '${pkg}' (part of qemu-guest-agent's dependency closure)" >&2
              exit 1
            fi
          done
        )

        # LIBGUESTFS_BACKEND=direct skips libguestfs's backend probe and
        # forces it onto /dev/kvm (made usable by the calling job's own
        # prepare-kvm-host step, or equivalent - see this action's own
        # description).
        export LIBGUESTFS_BACKEND=direct
        # Ubuntu ships /boot/vmlinuz-* mode 0600; virt-customize runs
        # unprivileged, so supermin needs it world-readable to build its appliance.
        sudo chmod 0644 /boot/vmlinuz-*
        guestfs_log="$(mktemp)"

        # 300s bound: guards a silent regression to a slow TCG fallback,
        # not a genuinely slow but successful run (normally well under a
        # minute on KVM).
        if ! timeout 300 virt-customize -a "${RUNNER_TEMP}/base.img" \
          --no-network \
          --write "${MARKER_PATH}:${MARKER_TOKEN}" \
          --copy-in "${qga_deb_dir}:/tmp" \
          --run-command 'dpkg -i /tmp/qga-debs/*.deb' \
          --verbose >"${guestfs_log}" 2>&1; then
          echo "::error::virt-customize failed or timed out; full --verbose output follows" >&2
          cat "${guestfs_log}" >&2
          exit 1
        fi

    # raw, not the qcow2 the cloud image ships as: the calling lane sets
    # IMAGE_INGEST_SOURCE_FORMAT=raw, and ingest rejects a source whose
    # actual format does not match it.
    - name: Convert the customized image to raw
      id: disk
      shell: bash
      run: |
        set -euo pipefail
        disk="${RUNNER_TEMP}/deploy-test.raw"
        qemu-img convert -O raw "${RUNNER_TEMP}/base.img" "${disk}"
        echo "disk-path=${disk}" >>"${GITHUB_OUTPUT}"

    # One name for all three objects: the ImageImport, the Image it
    # creates, and the prefix its content is named under
    # ("<image-name>-p<partition number>"). kezioctl defaults the last
    # two to --name, so only --name is given here.
    - name: Upload the deploy test image, request the import, and wait for its Image to reach Ready
      shell: bash
      env:
        IMAGE_NAME: ${{ inputs.image-name }}
        NAMESPACE: ${{ inputs.namespace }}
        TOKEN: ${{ inputs.token }}
        SERVICE_NAME: ${{ inputs.image-service-service-name }}
        LOCAL_PORT: ${{ inputs.local-port }}
        WAIT_TIMEOUT_SECONDS: ${{ inputs.wait-timeout-seconds }}
        DISK_PATH: ${{ steps.disk.outputs.disk-path }}
      run: |
        set -euo pipefail

        dump_diagnostics() {
          echo "::group::ImageImport ${IMAGE_NAME}"
          kubectl get imageimports.kezio.kojuro.date "${IMAGE_NAME}" -n "${NAMESPACE}" -o yaml || true
          echo "::endgroup::"
          echo "::group::Image ${IMAGE_NAME}"
          kubectl get images.kezio.kojuro.date "${IMAGE_NAME}" -n "${NAMESPACE}" -o yaml || true
          echo "::endgroup::"
          echo "::group::PartitionContents"
          kubectl get partitioncontents.kezio.kojuro.date -n "${NAMESPACE}" -o yaml || true
          echo "::endgroup::"
          echo "::group::ingest/publish Jobs/pods"
          kubectl describe jobs -n "${NAMESPACE}" -l "app.kubernetes.io/component=image-ingest-job" || true
          kubectl describe jobs -n "${NAMESPACE}" -l "app.kubernetes.io/component=partition-content-publish" || true
          kubectl logs -n "${NAMESPACE}" -l "app.kubernetes.io/component=image-ingest-job" --all-containers --tail=1000 || true
          kubectl logs -n "${NAMESPACE}" -l "app.kubernetes.io/component=partition-content-publish" --all-containers --tail=1000 || true
          echo "::endgroup::"
        }

        kubectl -n "${NAMESPACE}" port-forward "svc/${SERVICE_NAME}" "${LOCAL_PORT}:8080" &
        pf_pid=$!
        trap 'kill "${pf_pid}" 2>/dev/null || true' EXIT

        for _ in $(seq 1 30); do
          curl -sf -o /dev/null "http://127.0.0.1:${LOCAL_PORT}/healthz" && break
          sleep 1
        done

        trap 'dump_diagnostics; kill "${pf_pid}" 2>/dev/null || true' ERR
        bin/kezioctl image upload "${DISK_PATH}" \
          --name "${IMAGE_NAME}" \
          --namespace "${NAMESPACE}" \
          --server "http://127.0.0.1:${LOCAL_PORT}" \
          --token "${TOKEN}"

        # The import is polled beside the Image: a failed import (a bad
        # source, or a name it refuses to write over) never creates the
        # Image at all, so watching the Image alone would only ever
        # report the timeout.
        deadline=$(( $(date +%s) + WAIT_TIMEOUT_SECONDS ))
        while true; do
          import_state="$(kubectl get imageimports.kezio.kojuro.date "${IMAGE_NAME}" -n "${NAMESPACE}" -o jsonpath='{.status.state}' 2>/dev/null || true)"
          state="$(kubectl get images.kezio.kojuro.date "${IMAGE_NAME}" -n "${NAMESPACE}" -o jsonpath='{.status.state}' 2>/dev/null || true)"
          echo "imageimport ${IMAGE_NAME} state=${import_state:-<none>}, image ${IMAGE_NAME} state=${state:-<none>}"
          if [ "${state}" = "Ready" ]; then
            break
          fi
          if [ "${import_state}" = "Failed" ]; then
            echo "::error::ImageImport ${IMAGE_NAME} reached Failed" >&2
            dump_diagnostics
            exit 1
          fi
          if [ "${state}" = "Failed" ]; then
            echo "::error::Image ${IMAGE_NAME} reached Failed" >&2
            dump_diagnostics
            exit 1
          fi
          if [ "$(date +%s)" -ge "${deadline}" ]; then
            echo "::error::Image ${IMAGE_NAME} did not reach Ready within ${WAIT_TIMEOUT_SECONDS}s" >&2
            dump_diagnostics
            exit 1
          fi
          sleep 5
        done
        trap 'kill "${pf_pid}" 2>/dev/null || true' EXIT

        echo "Image ${IMAGE_NAME} reached Ready"
