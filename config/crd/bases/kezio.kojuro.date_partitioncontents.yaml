---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: partitioncontents.kezio.kojuro.date
spec:
  group: kezio.kojuro.date
  names:
    kind: PartitionContent
    listKind: PartitionContentList
    plural: partitioncontents
    singular: partitioncontent
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: PartitionContent is the Schema for the partitioncontents API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PartitionContentSpec defines the desired state of PartitionContent.
            properties:
              foo:
                description: Foo is an example field of PartitionContent. Edit partitioncontent_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: PartitionContentStatus defines the observed state of PartitionContent.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
