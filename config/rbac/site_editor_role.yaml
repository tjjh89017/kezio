# Not used by kezio itself; a helper for users who manage sites but
# should not control RBAC.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kezio
    app.kubernetes.io/managed-by: kustomize
  name: site-editor-role
rules:
- apiGroups:
  - kezio.kojuro.date
  resources:
  - sites
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kezio.kojuro.date
  resources:
  - sites/status
  verbs:
  - get
