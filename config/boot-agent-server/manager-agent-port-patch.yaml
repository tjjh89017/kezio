# Declares the agent registration server's containerPort so service.yaml's
# targetPort: agent-http resolves. The manager only listens on it once
# AGENT_SERVER_ADDR is set - left to the cluster operator or an e2e overlay.
- op: add
  path: /spec/template/spec/containers/0/ports/-
  value:
    containerPort: 8091
    name: agent-http
    protocol: TCP
